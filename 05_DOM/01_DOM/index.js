

const msg = document.getElementById('title')
alert(msg)

/* 
1) innerText   -> tex within the seclected tag/attributes but does not display the part that is displayed none 
2) innterHTML  -> gives those tags also which is inside the selected tag
3) textContent -> tex within the selected tag/attributes even some part that is displayed none 
 */

/*
selectors
1) getElementById
2) GetElementByClassName  -> returns html collection
3) querrySelector(#id/.class), querrySelector(p:first-child) -> retuns single value(i,e first )
4) querrySelectorAll -> returns node list

 */

/*
create a ul and change bg of fist element of ul
*/